
LATEXSOURCE=承诺书

EXTRA_DIST = $(LATEXSOURCE).tex

$(PDF):$(LATEXSOURCE).tex $(BIBSRC)
	$(MAKE) clean  compile
	$(MAKE) compile
	$(MAKE) compile
	$(MAKE) compile
distdir-local:
	hah

include ../common.mk

# makefile for thesis 
